
// Estrutura de repetição 

var num = 1;

// diferente do while o do while faz a verificação no final, ou seja, mesmo que a condição seja falsa ele vai repetir pelo menos uma vez
do{
    console.log(num);
    num++;

}while(num > 10);
